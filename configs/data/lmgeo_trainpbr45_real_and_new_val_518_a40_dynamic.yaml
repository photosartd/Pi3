# @package _global_

# Backward-compatible alias. Hardware and resolution are selected by the train
# profile; the dataset and validation definitions have a single canonical
# source in lmgeo_trainpbr45_real_and_new_val.yaml.
defaults:
  - lmgeo_trainpbr45_real_and_new_val
