# @package _global_

# Backward-compatible alias for the canonical all-train-PBR/BOP data config.
# Resolution, view sampling, and max_img_per_gpu come from the train profile.
defaults:
  - lmgeo_all_trainpbr_test_bop
